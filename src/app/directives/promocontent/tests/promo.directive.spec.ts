import { PromoDirective } from '../promo.directive';

describe('PromoDirective', () => {
  it('should create an instance', () => {
    const directive = new PromoDirective();
    expect(directive).toBeTruthy();
  });
});
